server:
  port: 8080

spring:
  application:
    name: epl-backend
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/epl}
    username: ${DB_USERNAME:epl}
    password: ${DB_PASSWORD:epl}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
  servlet:
    multipart:
      max-file-size: ${EPL_MULTIPART_MAX_FILE_SIZE:256MB}
      max-request-size: ${EPL_MULTIPART_MAX_REQUEST_SIZE:256MB}
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

logging:
  file:
    name: logs/epl-backend.log
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 14
  level:
    ch.marcovogt.epl: INFO

# Operational defaults
# Topic filters include both canonical EPL topics (epld/{deviceId}/...) and observed Shelly capture topics (epld01/...)
epl:
  feed:
    admin-buffer-size: ${EPL_ADMIN_BUFFER_SIZE:500}
  device:
    offline-timeout: ${EPL_DEVICE_OFFLINE_TIMEOUT:PT25S}
    offline-check-delay-ms: ${EPL_DEVICE_OFFLINE_CHECK_DELAY_MS:5000}
  mqtt:
    broker-uri: ${MQTT_BROKER_URI:tcp://localhost:1883}
    client-id: ${MQTT_CLIENT_ID:epl-backend}
    username: ${MQTT_USERNAME:}
    password: ${MQTT_PASSWORD:}
    qos: ${MQTT_QOS:1}
    reconnect-delay-ms: ${MQTT_RECONNECT_DELAY_MS:5000}
    clean-session: true
    append-random-client-suffix: true
    topic-filters:
      - epld/+/event/#
      - epld/+/status/#
      - epld/+/ack/#
      - +/event/#
      - +/command/#
      - +/events/rpc
      - +/telemetry
      - +/online
      - +/status/#
  auth:
    session-ttl: PT8H
    presence-window: PT45S
    cleanup-delay-ms: 60000
  log-mode:
    kafka:
      enabled: ${EPL_LOG_MODE_KAFKA_ENABLED:false}
      bootstrap-servers: ${EPL_LOG_MODE_KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
      topic: ${EPL_LOG_MODE_KAFKA_TOPIC:epl.events.log}
      client-id: ${EPL_LOG_MODE_KAFKA_CLIENT_ID:epl-log-mode}
      replay-default-max-records: ${EPL_LOG_MODE_KAFKA_REPLAY_DEFAULT_MAX_RECORDS:200}
  cloudflare:
    enabled: ${EPL_CLOUDFLARE_ENABLED:false}
    hostname: ${EPL_CLOUDFLARE_HOSTNAME:}
    ready-url: ${EPL_CLOUDFLARE_READY_URL:http://cloudflared:2000/ready}
    metrics-url: ${EPL_CLOUDFLARE_METRICS_URL:http://cloudflared:2000/metrics}
    request-timeout: ${EPL_CLOUDFLARE_REQUEST_TIMEOUT:PT2S}
